# --- web server ---
fastapi>=0.112
uvicorn[standard]>=0.30

# --- core job logic ---
earthengine-api>=1.0.0
google-cloud-storage>=2.17
pydantic>=2.8
python-multipart>=0.0.9
aiofiles>=24.1.0

# --- geometry / file parsing WITHOUT GDAL ---
shapely>=2.0          # manylinux wheels (no GEOS system install needed)
pyogrio>=0.9.0        # ships wheels that bundle GDAL – no system GDAL
fastkml>=0.12
simplekml>=1.3.6
geojson>=3.1.0
